{
  "entities": {
    "NewsletterSubscriber": {
      "title": "NewsletterSubscriber",
      "description": "An email subscription to the portal announcements and news.",
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "description": "The subscriber's email address"
        },
        "subscribedAt": {
          "type": "string",
          "description": "ISO timestamp of when the subscription was made"
        },
        "active": {
          "type": "boolean",
          "description": "Whether the subscription is currently active"
        }
      },
      "required": ["email", "subscribedAt", "active"]
    }
  },
  "firestore": {
    "newsletter_subscribers/{subscriberId}": {
      "schema": "NewsletterSubscriber",
      "description": "Collection of email subscriptions for news and bulletins."
    }
  }
}
